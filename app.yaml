application: bukidutility
version: 1
runtime: go
api_version: go1

handlers:

# All other routes. IMPORTANT: This should always be the last route listed.
- url: /.*
  script: _go_app
  login: optional
